configfile: "config/config.yaml"


include: "rules/common.smk"
include: "rules/precomputed.smk"
include: "rules/model.smk"


PRECOMPUTED_MODELS = ["GPN-MSA_LLR"]


rule all:
    input:
        expand(
            "results/metrics/{dataset}/{model}.parquet",
            dataset=config["datasets"].keys(),
            model=config["models"],
        ),
        # expand(
        #     "results/features/{dataset}/{model}.parquet",
        #     dataset=config["datasets"].keys(),
        #     model=PRECOMPUTED_MODELS,
        # )
